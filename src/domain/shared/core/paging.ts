/**
 * Object search paging
 * @argument key: value
 *
 * @description you may use key as string with number or string value
 *
 * @example { offset: 0, limit: 0 }
 */
export type Paging = {
  offset: number,
  limit: number
}
